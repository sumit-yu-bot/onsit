# 🔍 LeakOSINT CLI Tool

A powerful command-line interface for searching leaked data using the LeakOSINT API.

## ⚡ Quick Start

### Installation

```bash
# Clone the repository
git clone https://github.com/yourusername/leakosint-cli.git
cd leakosint-cli

# Install dependencies
pip install -r requirements.txt

# Install the tool
pip install -e .
